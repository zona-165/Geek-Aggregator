"use client";
import { FormEvent, useState } from "react";
export default function Login() { const [password, setPassword] = useState(""); const [error, setError] = useState(""); async function submit(e: FormEvent) { e.preventDefault(); const r = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ username: "admin", password }) }); if (r.ok) location.href = "/admin"; else setError("密码错误"); } return <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", background: "#f6f7fb", fontFamily: "system-ui" }}><form onSubmit={submit} style={{ width: 340, padding: 32, borderRadius: 22, background: "white", boxShadow: "0 20px 60px #17203318" }}><div style={{ color: "#68728a", letterSpacing: 2, fontSize: 12 }}>CONTENT LAB · ADMIN</div><h1>后台登录</h1><p style={{ color: "#68728a" }}>管理员：admin</p><input autoFocus type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="请输入密码" style={{ width: "100%", boxSizing: "border-box", padding: 13, border: "1px solid #dce1ea", borderRadius: 10 }} />{error && <p style={{ color: "#d34b5f" }}>{error}</p>}<button style={{ marginTop: 16, width: "100%", padding: 13, border: 0, borderRadius: 10, background: "#1d2638", color: "white" }}>登录后台</button></form></main>; }
